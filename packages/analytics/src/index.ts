// @kyno/analytics — Event tracking SDK
// Phase 2: track(), identify(), screen(), etc.

export function track(_event: string, _properties?: Record<string, unknown>) {
  // Will be wired to Mixpanel / Amplitude / PostHog in Phase 2
}
